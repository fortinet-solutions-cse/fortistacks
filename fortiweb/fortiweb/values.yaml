# Default values for fortiweb.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 3

image:
  repository: fortistackscontainerregistry.azurecr.io/fortiweb
  pullPolicy: IfNotPresent

imagePullSecrets: [ {"name:", "fwbregistrysecret"} ]
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

podSecurityContext: {}
  # fsGroup: 2000

securityContext:
  capabilities:
    add: ["NET_ADMIN", "NET_RAW", "SYS_TIME", "SYS_PTRACE" ]
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80
  portssl: 443

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
   limits:
     cpu: 2
     memory: 4096Mi
   requests:
     cpu: 700m
     memory: 1024Mi

nodeSelector: {}

tolerations: []

affinity: {}


# from https://helm.sh/docs/howto/charts_tips_and_tricks/
imageCredentials:
  registry: fortistackscontainerregistry.azurecr.io
  username: fortistacksContainerRegistry
  password: br+JMM8h3mMOjPsahHcj1NAqNC8Hny4U
  email: tregistry@azureacr.org
# use: az acr credential show --name fortistacksContainerRegistry